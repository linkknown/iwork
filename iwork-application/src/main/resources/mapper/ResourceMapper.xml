<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.linkknown.iwork.mapper.ResourceMapper">

    <select id="queryPageResourceList" parameterType="map" resultType="com.linkknown.iwork.entity.Resource">
        select * from resource
        <if test="map.search != ''">
            where resource_name like concat('%',#{map.search},'%')
            or resource_type like concat('%',#{map.search},'%')
            or resource_link like concat('%',#{map.search},'%')
        </if>
    </select>

    <select id="queryAllResources" resultType="com.linkknown.iwork.entity.Resource">
        select * from resource
        <where>
            <if test="appId != null and appId > 0">
                and app_id = #{appId}
            </if>
            <if test="resourceType != null and resourceType != ''">
                and resource_type = #{resourceType}
            </if>
        </where>

    </select>

    <select id="queryResourceById" resultType="com.linkknown.iwork.entity.Resource">
        select * from resource where app_id = #{appId} and id = #{id}
    </select>

    <select id="queryResourceByName" resultType="com.linkknown.iwork.entity.Resource">
        select * from resource where app_id = #{appId} and resource_name = #{resourceName}
    </select>

    <delete id="deleteResource">
        delete from resource where id = #{id}
    </delete>

    <insert id="insertOrUpdateResource" parameterType="com.linkknown.iwork.entity.Resource">
        <selectKey keyProperty="count" resultType="int" order="BEFORE">
            select count(*) as count from resource where id = #{resource.id}
        </selectKey>
        <if test="count > 0">
            update resource
            set app_id = #{resource.appId},
            resource_name = #{resource.resourceName},
            resource_type = #{resource.resourceType},
            resource_link = #{resource.resourceLink},
            last_updated_time = #{resource.lastUpdatedTime}
            where id = #{resource.id}
        </if>
        <if test="count==0">
            insert into resource (
            app_id,
            resource_name,
            resource_type,
            resource_link,
            last_updated_time
            ) values(
            #{resource.appId},
            #{resource.resourceName},
            #{resource.resourceType},
            #{resource.resourceLink},
            #{resource.lastUpdatedTime}
            )
        </if>
    </insert>
</mapper>