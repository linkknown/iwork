<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.linkknown.iwork.mapper.OperateLogMapper">



    <insert id="insertOperateLog" parameterType="com.linkknown.iwork.entity.OperateLog">
        insert into operate_log (
        user_name,
        ip_addr,
        log,
        created_time
        ) values(
        #{operateLog.userName},
        #{operateLog.ipAddr},
        #{operateLog.log},
        #{operateLog.createdTime}
        )
    </insert>

</mapper>